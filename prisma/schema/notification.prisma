model Notification {
    id           String   @id @default(uuid())
    title        String
    message      String
    receiverId   String?
    receiverRole Role
    generatedAt  DateTime @default(now())
}
